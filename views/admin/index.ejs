<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="../src/css/output.css">

    
</head>
<body class="w-screen h-screen overflow-hidden bg-gray-50">
    <main class="w-full h-full p-4 grid place-content-center">
        <form action="/admin" method="POST" class="w-[22rem]">
            <div class="border rounded-xl p-4 lg:p-6">
                <div class="mb-4 flex flex-col items-center">
                    <h2 class="text-blue-950 font-medium text-xl ">connectez-vous !!!</h2>
                    
                </div>
                
                <div class="flex flex-col">
                    <label for="">Login</label>
                    <input type="email" name="email" class="border p-2 mt-2 rounded-lg outline-none focus:border-blue-950/70 duration-300" placeholder="Votre email" id="">
                </div>
                <div class="flex flex-col mt-4">
                    <label for="">Password</label>
                    <input type="password" name="password" class="border p-2 mt-2 rounded-lg outline-none focus:border-blue-950/70 duration-300" placeholder="Votre mot de pass" id="">
                </div>
                <!-- error messages -->
                <div class="mt-4">
                    <p class="text-red-600 "><div style="width:400px">
                        <% if(error && error.length>0){ %>
                        <div class="warning-message">
                            <div class="header">
                                <%= error %>
                            </div>
                        </div>
                        <% } %>
                        <% if(success && success.length > 0) { %>
                        <div class="success-message">
                            <div class="header">
                                <%= success%>
                            </div>
                        </div>
                        <% } %>
                    </div></p>
                    <p class="text-red-600 "></p>
                    <p class="text-red-600 "></p>
                </div>
                <button type="submit" title="submit button" class="bg-blue-950 text-white p-2 mt-4 w-full rounded-lg">Submit</button>
            </div>
        </form>
    </main>
</body>
</html>